# Development Dockerfile — hot reload via ts-node (local Docker only)
FROM node:20-alpine

WORKDIR /app

RUN corepack enable && npm install -g nodemon ts-node

COPY package.json yarn.lock ./
RUN yarn install --frozen-lockfile

COPY . .

RUN mkdir -p uploads

EXPOSE 3006

CMD ["nodemon", "src/index.ts"]
