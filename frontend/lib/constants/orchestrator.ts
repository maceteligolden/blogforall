enum CONVERSATION_STATUS {
  SPEAKING = "speaking",
  THINKING = "thinking",
  LISTENING = "listening",
  IDLE = "idle",
}

enum CHAT_ROLES {
  USER = "user",
  ASSISTANT = "assistant",
  TOOL = "tool",
  SYSTEM = "system",
}

export { CONVERSATION_STATUS, CHAT_ROLES };
